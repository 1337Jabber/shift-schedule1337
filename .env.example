# Vercel Postgres connection string.
# Vercel injects POSTGRES_URL automatically when you link a storage instance.
# For local testing: copy to .env and fill in.
POSTGRES_URL=postgres://user:password@host:5432/verceldb

# Optional: sets the admin password on first boot (default: admin123).
# Change it right after linking the project!
ADMIN_PASSWORD=change-me